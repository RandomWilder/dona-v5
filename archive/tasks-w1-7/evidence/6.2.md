# Evidence — Slice 6.2 · An apartment, its spaces, and the bays it implies

**Closed:** 2026-09-13 · **Planned window:** week 6 (Sun 11 Oct) · **Plan mode:** no — one module, no
migration, no kernel, no new dependency. **Mockup waived by the director**, so `mockups/unit-new.html`
was never painted and guard four stayed idle.

## What was proved

**527** code + **41** hooks + **54** policy · **0 failed** (524/41/54 at 6.1). 4 grep guards, 0
violations. `typecheck` · `lint` · `test` · `test:policy` · `guards` green.

**Clicked on a restarted process, twice** — 6.1's listener (pid **14000**) killed, pid **17203**,
then pid **17608** after the fix below; `/health` 200 both. Building `01a0774c`: spaces **44 → 47**,
units **44 → 45** for one flat, because an apartment is a `UNIT` space, a `חניה` and a `מחסן`. The
page then carried `דירה 999`, `פנויה`, `חניות · 1`, `מחסנים · 1`.

**The same number twice is one flat.** Re-posted `999`: **47/45 unchanged**, `rooms 3.5 → 4`, `area
81.5 → null`, `READY → RENOVATION`, בדק `null → 2028-01-01`. The guarantee is `space (building_id,
space_kind, name)` and R2, so the form checks nothing. Rows deleted after; local DB back at **44/44**.

**OPERATOR refused on the live process.** Door on the building page: **0**. GET `…/units/new`
**403**; POST **403** `{ code: not_allowed, message: not_allowed }`; **0** rows.

**Rejected at the edge, 0 rows:** blank `unit_number`, `rooms` as words, `rooms = -3`,
`area = 78,5`, `condition_status = PALACE`, a non-ISO בדק date — all **400 `invalid`**. A malformed
building id is `invalid`, an absent one `not_found`.

## Red first, and red for the right reason

- **The refusal:** `estate.write` granted to `OPERATOR` in `MATRIX` → **`actual: 303, expected:
  403`** (A11's case went red with it — the same line refusing). Grant removed → 403.
- **The project:** `projectCode: null` in the rebuilt building → **`actual: null, expected:
  01a0774c-…`**. `upsertUnitRow` upserts the building and `DO UPDATE` sets `project_id` from what it
  is handed, so a form-shaped building would unlink the project while adding a flat.
- **The blank floor**, found by reading the diff and not by a test — every case happened to send one.
  `optionalText` answers null for an *absent* key and `invalid` for `''`, which is what an untouched
  input posts. `blankToNull` added, case reverted → **`actual: 400, expected: 303`**.

## Structural calls

- **Flow A13, not A12.** `roadmap.md` and `todo.md` both give **A12** to 6.3's intake; A11's prose
  calling this screen A12 was the stale half and is corrected in place, with the reason.
- **The `UNIT` space is named by the bare `unit_number`**, as the register importer names it —
  `דירה 12A` from a form and `12A` from a CSV would be two apartments behind one door.
- **The building is rebuilt from its own row**, which is A11's rule about the project one level up.
- **The POST owns a transaction:** four statements for one flat, and a failure between them leaves a
  `UNIT` space with no unit — legal here (a lobby is one) and visible as a דירות count with no card.
- **No new estate command.** `upsertUnitRow` has its second caller and needed no new argument, which
  is the test of whether 2.4 extracted it or copied it.

## Carried → 6.3

`inTransaction` is now **written twice** (`src/evidence/internal/promote.ts`, inline in estate's
routes): **a third writer moves it to the kernel**. `.check` joins `.form-grid` / `.form-row` /
`.hint` / `.form-actions` at two files each: **a third occurrence moves them to `tokens.css`**. Both
land on 6.3, which writes. Unchanged elsewhere: `config_settings` editor parked (6.4 seeds fields);
staging `staff:add` + the 5.6 clock-end click → **6.7**; `work.ts` after the console walk.
