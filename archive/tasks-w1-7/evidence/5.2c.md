# Evidence — Slice 5.2c · v3's ops shell on v5's live URLs

**Closed:** 2026-09-10 · **Planned window:** week 5 (after 5.2b, before 5.3) · **Plan mode:** yes —
kernel page frame plus every signed-in screen.

> **5.2b put one chrome on every screen and left it a top bar.** The temp admin in v3 was already
> a dark ops sidebar on the same token sheet. This slice is that frame, with v5's live destinations
> only. Unbuilt tabs stay 5.9. Login has no rail. Nothing was pushed.

## What was proved

| | |
|---|---|
| Tests at close | **489** code + **41** hooks + **50** policy · **0 failed**, 0 skipped |
| Grep guards | 3, 0 violations |
| New runtime dependencies | **none** |
| New migration | **none** |

`npm run typecheck` · `npm run lint` · `npm test` · `npm run test:policy` · `npm run guards` all
green. No prompt, model, retrieval or tool change.

## The acceptance bar

**Done when: every authenticated screen is the ops sidebar.**
`tests/ui/tokens.test.ts` requires `class="ops"`, the live hrefs (buildings, expiring, incomplete,
search, staff), the sign-out POST with a non-empty csrf, and exactly one `<a aria-current="page">`
except on the root index, which has none. Search in the rail is a link to `/estate/search`, not a
GET form. Nested building, unit and document screens mark estate.

**Done when: login has no rail.** No `.ops`, no estate href, no logout.

**Verify: restart `npm run dev` and sign out from a non-index screen.**
New process on `:3000`. Against it, a minted local session: ops present on `/`, `/estate`
(current: buildings), `/estate/expiring`, `/estate/search`, `/staff`. `POST /staff/logout` → **303**
login; next `GET /estate` → **303** login. Login HTML has no `.ops`.

**Not done, on purpose:** push, PR, staging. Approve the look on localhost first.

## What this slice did not do

Card restyle. v3's hamburger script. v3's seven destinations (queue, conversations, …). 5.3 and
5.9's unbuilt tabs.
