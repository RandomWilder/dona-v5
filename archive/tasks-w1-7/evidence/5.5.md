# Evidence — Slice 5.5 · Amendment log + party-identity ruling

**Closed:** 2026-09-11 · **Planned window:** week 5 · **Plan mode:** yes (tenancy + estate).

> Demo first half: an amendment on a generated unit is readable on that unit. A2 still does not
> match a name across tenancies.

## What was proved

| | |
|---|---|
| Tests at close | **502** code + **41** hooks + **54** policy · **0 failed** |
| Grep guards | 4, 0 violations (mockup guard idle after `changelog.html` deleted) |
| New runtime dependencies | **none** |
| New migration | **none** |

`npm run typecheck` · `lint` · `test` · `test:policy` · `guards` green.

## The acceptance bar

**Change log.** `listTenancyEvents(unitId)` — no party, no phone. Unit page shows field, old → new,
operator email, document link, in order. Empty is legal. Never-a-name **kept**.

**A2 step 5 kept.** Local portfolio: **2,871** identified parties, **0** nameless, **0** nameless
same-name groups. **303** identified names collide (fixture name pool, keyed by `national_id_key`).
`createParty` still inserts twice for the same name. No policy case: the prohibition did not move.

**`superseded_by` still omitted.** **6** mapping rows, **2** targets (`tenancy.start_date` × 2,
`tenancy.end_date` × 4), **0** live stamps. Invariant 2 still covers the cases.

**Verify.** Inject on generated דירה 1 · אשחר 24: 200, `2028-01-17 → 2029-01-17`,
`ops@example.test`, document href. No cookie → 303 `/staff/login`. Painted mockup served, then
deleted. **Director clicked `:3000` 11 Sep** after restart — same unit, same יומן.

## Carried → 5.6

Clock-driven `terminated` with a null document; `amended` still names paper.
