# Evidence — Slice 5.3 · The rest of the ops destinations

**Closed:** 2026-09-10 · **Planned window:** week 5 (after 5.2d) · **Plan mode:** no — composition
root chrome and two stub routes; kernel learns no path.

> The rail had five live destinations. This slice adds קריאות and הגדרות as stubs that name the
> week and slice that own them. 5.9 is never used. `national_id` waits on week 9.

## What was proved

| | |
|---|---|
| Tests at close | **496** code + **41** hooks + **54** policy · **0 failed** |
| Grep guards | 4, 0 violations (mockup guard idle: 0 html files) |
| New runtime dependencies | **none** |
| New migration | **none** |

`npm run typecheck` · `npm run lint` · `npm test` · `npm run test:policy` · `npm run guards` all
green. No prompt, model, retrieval or tool change.

## The acceptance bar

**Done when: every signed-in rail names seven destinations.**
`tests/ui/tokens.test.ts` requires `/calls`, `/settings`, קריאות and הגדרות on every signed-in
screen in the registry. `/calls` names `שבוע 7 · סלייס 7.2`. `/settings` names `שבוע 5 · סלייס 5.8`.
`data-state="wired"` on those two; the IA paint is `data-state="painted"`.

**Done when: login has none of it.** `/calls` with no cookie → **303** `/staff/login`. Login HTML
has no those hrefs.

**Verify: `/dev/mockups/:flow` only on a `-dev` process.** `9.9.9-test` does not register it.
`devMockups: true` registers `estate.read`; no session → 303 login. Restarted `npm run dev` on
`:3000`; `/calls` in the browser landed on login, with no ops rail.

## What this slice did not do

Card restyle. Live settings writes (5.8). The ticket walk (7.2). The `national_id` policy case
(week 9). Signed-in click-through still needs a session (Google).
