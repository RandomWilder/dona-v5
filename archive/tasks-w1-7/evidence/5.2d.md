# Evidence — Slice 5.2d · Phone shell: the ops rail is a drawer below 840px

**Closed:** 2026-09-10 · **Planned window:** week 5 (after 5.2c, before 5.3) · **Plan mode:** yes —
kernel page frame; staff login card width.

> **5.2c left the rail wrapping on a phone.** This slice puts it behind a menu control in the same
> shell every signed-in screen already uses, still with no `<script>`. Login has no menu. Nothing
> was pushed.

## What was proved

| | |
|---|---|
| Tests at close | **489** code + **41** hooks + **50** policy · **0 failed**, 0 skipped |
| Grep guards | 3, 0 violations |
| New runtime dependencies | **none** |
| New migration | **none** |

`npm run typecheck` · `npm run lint` · `npm test` · `npm run test:policy` · `npm run guards` all
green. No prompt, model, retrieval or tool change.

## The acceptance bar

**Done when: below 840px a signed-in screen shows a menu control, not the full rail.**
`tests/ui/tokens.test.ts` requires `id="nav-toggle"`, `class="ops-menu"`, and תפריט on every
signed-in screen in the registry, and none of those on login. Destinations and sign-out stay in
the rail HTML; they are off-canvas and `visibility: hidden` until the control is on.

**Done when: login has neither.** Live `:3000/staff/login` at **375px**: no `.ops`, card width
**317px**, does not overflow.

**Verify: restart `npm run dev`; open the drawer at 375px, then the desktop rail.**
New process on `:3000`. Phone: sticky bar (תפריט / סגירה), drawer from the inline-start, scrim,
destinations and יציאה inside. Desktop **1280px**: bar `display: none`, rail `position: static`.
Closed drawer links are not in the tab order.

**Not done, on purpose:** push, PR, staging. A signed-in click-through on `:3000` still needs a
session (Google). Unbuilt tabs stay 5.9.
