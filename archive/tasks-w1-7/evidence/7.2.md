# Slice 7.2 — The declaration becomes editable (ADMIN)

Closed 15 Sep 2026. No migration. Flow **A14**, written into `SPEC-flows.md` before the code.

Foundation rule 8 — a type is a row and a field is a row, costing no migration and no deploy — has
been half true since 3.1. The field half cost a commit to `fixtures/document-types.ts` and a
`seed:doctypes`, paid three times (3.1, 3.5, 6.4). It is true now.

`POST /documents/types/:typeKey/fields`, `settings.write`, ADMIN only; `roles.ts` untouched.
`declareDocumentTypeField` / `retireDocumentTypeField` are new. **`upsertDocumentTypeField` was not
touched** — its `ON CONFLICT … DO UPDATE` is the seed's re-apply and is the edit R18 forbids at run time.

**The todo's bullet was wrong and was corrected rather than implemented.** It said close at
`effective_to = <clock date>` and open at `effective_from = <clock date>`; `documentTypeFields` is
inclusive at both ends, so that leaves **two live rows for one field today** — the field printed
twice and handed to the model twice. The close is the day before, as the seed has always done.

`address` corrected on `:3000`, from `psql`:

```
 address | 2026-09-07 | 2026-09-07 | המושכר
 address | 2026-09-08 | 2026-09-13 | רחוב + בניין מספר + עיר…   ← closed the day before
 address | 2026-09-14 |            | רחוב + בניין מספר בלבד…    ← opened today
```

One row governs today, the superseded one still says what it said. `city` declared from the form:
8 fields → 9. `guarantor_name` retired: 9 → 8 on screen, its one row closed at 2026-09-13 —
deactivate, never delete. Re-declaring `address` the same day: `409 conflict`, naming R18.

## The two refusals, both red first

**Money** — `tests/policy/money-field.test.ts`, new. Red: `Missing expected rejection: rent_amount`.
It builds five violations an admin could type, asserts the live catalogue is clean, and reads
`MONEY_KEY_TOKENS` / `MONEY_LABEL_TERMS` off the guard itself, so shortening the vocabulary is red.
On `:3000`: `{"code":"invalid","message":"a declaration may not name money — field_key carries
\"amount\". Foundation rule 2 … adding one is a migration, a diff and a review — never a form."}`

**Role** — registered at `documents.write` first, which an OPERATOR holds:
`✖ an OPERATOR is refused — 303 !== 403`, `floor_area` declared. Green at `settings.write`. **A route
case, not a policy case, against the todo's wording**: `tests/policy/` builds no app, and every role
refusal here is asserted against the stance the composition root registered. Locally as OPERATOR:
`GET` 200, `אדמין בלבד` **0**, retire buttons **0**, rows **8**; `POST` **403** and nothing more;
`floor_area` rows after **0**.

## Numbers

`test:code` **640 pass / 0 fail / 0 skipped**; typecheck, lint, guards clean. Audit after the click,
`around` recording refusals too: `declare ok city` · `declare invalid rent_amount` · `declare ok
address` · `declare conflict address` · `retire ok guarantor_name`. **375px: `scrollWidth` 375, no
horizontal overflow** — 7.1's `minmax(0, 1fr)` holds with a form and a tenth column added.
**Gate two was not run locally**: the corpus cases need `OPENAI_API_KEY`, absent since 7.1. CI runs it.

## What it cost to find

**A refusal case can pass as a 303.** `SESSION_ABSOLUTE_MS` is twelve hours (5.1) and this is the
first route suite spanning two days, so day one's session was expired on day two and the guard
answered a redirect to sign-in — indistinguishable from a route that accepted the post. Four refusal
cases were "passing" that way. Each day mints its own session of the same account now.

**The day is UTC and the office is not.** The click ran at 00:02 IDT and the screen stamped the
declaration **2026-09-14**. Raised, not fixed: it is the module's convention, not this route's — and
for three hours a night the twice-in-one-day conflict can be stepped around. **Counted afterwards,
on the director's ruling of 15 Sep: eleven call sites, and one is
`src/scope/internal/isolation-join.ts:187` — foundation rule 1's *Tenancy (active today)*.** Between
midnight and 03:00 IDT the isolation join asks about yesterday. That is slice **7.2b**, ahead of 7.3.

Developer catalogue restored to the seed by SQL after the click; nothing had been extracted under
those rows. `.env`'s two `DOCUMENT_AI_*` lines are **removed**, which is that carry's own
instruction — `.env.bak-6.8` restores them.
