# Evidence — Slice 4.6b · Confirm picks an existing terms profile

**Date:** 2026-09-08 · **Week 4** · **Size:** S

> **Done when:** the field is a select of `terms_profile.name` rows; empty list withholds write and
> does not insert.
> **Verify:** select with an existing name; empty list has no submit; confirm still looks up by name.

## The numbers

```
                               before (4.6a)           after (4.6b)
npm test — code                437                     440          (+3)
npm test — hooks               41                      41
tests/policy/                  44                      44           (no new case)
biome check                    145 files               145 files
guards                         3 · ok                  3 · ok       (19 migrations scanned)
typecheck                      clean                   clean
runtime dependencies           5                       5
migrations                     19                      19           (none)
evals                          unchanged               not re-run   (no prompt/tool change)
```

Three new tests: `listTermsProfiles` returns an inserted name; confirm HTML is a `<select>` of
existing names with a submit; empty list shows the missing-annex lede and no submit.

## What was proved

- **No seed.** Local `terms_profile` already had two rows (`נספח תחזוקה — תקן`,
  `נספח תחזוקה — מורחב`), from the register fixture. Confirm lists those. Inventing a third because
  staging once typed a name that was not in the table would have answered the client question 2.5
  still owns.
- **Select, not a typed field.** `listTermsProfiles` is names only, ordered. POST still looks up by
  the posted name; a missing name is still `invalid`. The blank first option is required, so one
  existing annex is not silently chosen.
- **Empty list withholds the write.** No insert of `standard`. The page says the register has not
  been imported yet.
- **Phone-shaped flake.** The upload-screen “never a 05 number” assertion was matching a ULID
  (`a053-07…`). Tightened to seven digits after the prefix so the catalogue screen is not a coin
  flip.

## Verify

```
npm run typecheck                          ok
npm run lint                              145 files
npm run guards                             3 guards, 0 violations
npm test                                   440 code + 41 hooks, 0 fail
npm run test:policy                        44, 0 fail
  listTermsProfiles includes inserted name
  select + אישור וכתיבה when names exist
  empty names: lede, no submit
```

Local click-through of a real PDF on `:3000` is the director’s before staging — this slice’s HTML
is covered by the renderer tests; OCR on a scan still needs the serving app. **Pipeline §8 now
requires a restarted `npm run dev` before merge** for any screen change; 4.6b closed before that
rule existed.

## Carry

- **Which `terms_profile` a real lease is on** — still week 5 / 2.5. Staging must already hold the
  real names (register import). If the dropdown is empty there, import the register; do not seed a
  fake annex.
- A3 addendum — **4.7**.
- Typed **מי מאשר** until week 5’s session.
- JSON error dump on a refused confirm — UI parked at M1.
- Staging `ocr:sweep` — still director.
- F6’s three acts — block **4.5** only.
