# Evidence — Slice 5.7 · Obligation and ObligationType

**Closed:** 2026-09-12 · **Planned window:** week 5 · **Plan mode:** yes (migration + two entities).

> Last two month-one entities. Catalogue never deleted; who is responsible is snapshotted.

## What was proved

| | |
|---|---|
| Tests at close | **515** code + **41** hooks + **54** policy · **0 failed** |
| Grep guards | 4, 0 violations (mockup guard idle — no screen) |
| New runtime dependencies | **none** |
| New migration | **`0026_obligation.sql`** — `obligation_type`, `obligation`; DELETE trigger |

`npm run typecheck` · `lint` · `test` · `test:policy` · `guards` green.

Public base tables **27** (includes `schema_migrations`). Two new relations. Seed is not in the
migration.

## The acceptance bar

**Create, edit the type, read back.** `createObligation` copies `default_responsible_party`. After
the type is flipped to `OPERATOR` and deactivated, the obligation still says `TENANT`. Status
stayed `MISSING` (evidence still required, still absent).

**DELETE refused, red first.** Before `0026_`, `obligation_type` was `42P01`. After: DELETE of a
type with no children is **23001** (`obligation_type is deactivated, never deleted`). No delete on
the contract.

**Second catalogue apply:** 5 created then 0 created / 5 updated, on unique codes so the count is
about this run.

## Derived status (read, not stored)

60-day window. `MISSING` if evidence required and absent; else `EXPIRED` / `EXPIRING` / `SATISFIED`
from `valid_to` vs the injected clock's UTC day. No amount column.

## Carried → 5.8

Settings screen (both catalogues). Staging clock-end click still owed after 5.6 merge. `work.ts`
still post-7.2. Five seed codes exist as data (`npm run seed:obligation-types`, no workflow).
