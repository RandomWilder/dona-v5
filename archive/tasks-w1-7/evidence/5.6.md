# Evidence — Slice 5.6 · Clock-driven tenancy end

**Closed:** 2026-09-11 · **Planned window:** week 5 · **Plan mode:** yes (migration + tenancy/estate).

> Demo second half: a lease ends because a date passed, with no document.

## What was proved

| | |
|---|---|
| Tests at close | **503** code + **41** hooks + **54** policy · **0 failed** |
| Grep guards | 4, 0 violations (mockup guard idle — no new flow) |
| New runtime dependencies | **none** |
| New migration | **`0025_tenancy_event_terminated.sql`** — kind `amended \| terminated`; `amended_names_its_document` kept; `terminated_has_no_document` added |

`npm run typecheck` · `lint` · `test` · `test:policy` · `guards` green.

## The acceptance bar

**Both directions, one schema test, red first.** `terminated` + null document accepted (was
`tenancy_event_kind_check` 23514 against 0019). `amended` + null document still 23514.
`terminated` + a document refused by the new CHECK.

**Clock.** Last day of an ACTIVE lease: still ACTIVE, 0 events. Advance 24h: `ENDED` + one
`terminated` row, actor `system`, `source_document_id` null, `at` = clock. Second call: still one
row.

**Unit page.** Reconciles due endings before the log. Inject (clock 2026-09-12, end 2026-09-11):
**200**, `ACTIVE → ENDED`, `system`, no מסמך link. No cookie → 303 `/staff/login`. Never-a-name
**kept**. `npm run dev` restarted 11 Sep (pid leftover from 5.5; `:3000` `/health` up).

Local portfolio had **0** ACTIVE rows past today, so the click on a generated unit waits on an
amendment of `end_date` to yesterday — staging demo, date said out loud.

## Carried → 5.7

Obligation and ObligationType. Staging click still owed after merge. `work.ts` still post-7.2.
