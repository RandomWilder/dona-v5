# Evidence — Slice 6.4 · ת.ז. on the capture path

**Closed:** 2026-09-13 · **Planned window:** week 6 (Sun 11 Oct) · **Plan mode:** yes — four
documents and one module. No migration, no new dependency, no change to `EXTRACT_INSTRUCTIONS`.

## What was proved

**554** code + **41** hooks + **54** policy · **0 failed** (547/41/54 at 6.3). 4 guards, 0 violations.
`typecheck` · `lint` (180 files) · `test` · `test:policy` · `guards` green. `evals` local **1/3, 0
failed, 2 skipped** — no `OPENAI_API_KEY` here; CI's `evals` job is the gate and passed on the spec
half.

**The spec was merged before the code**, two PRs. **ADR-0006 amends ADR-0004 decision 2 only** —
without it slice **9.1** masks the value this slice exists to capture.

**Two seed rows, no migration.** `seed:doctypes` → types 0 created / 10 updated, fields 0 / **22**.
`lease` now carries **12** field rows, the two new at `effective_from` **2026-09-13**, both
`is_required = false` — 6.5's third acceptance case written into the schema. `field_promotion` rows
targeting either: **0**.

**Withheld by default.** `renderReadPage` takes a **required** `mayReadIdentifiers`; the compiler
found all five call sites. `IDENTIFIER_FIELD_KEYS` is a named set and not a shape test — week 5's
`/05\d/` is what a shape test costs.

**Red first, twice.** Gate removed from `visibleRows` → `expected: /312345678/` on the registry case.
`mayReadIdentifiers` forced `true` → `an OPERATOR sees a count` failed, 26/2. Restored: 28/0.

**One OCR call, not two** — 6.3's carry, discharged. A call-counting spy on the A12 intake path, the
way 6.3 counted puts: **`ocrCalls === 1`** for a scanned lease. `IntakeRequest` gained `readPages`;
`fileDocument` is one optional field wider and no responsibility wider.

## On `:3000`, restarted (pid 27709, `/health` 200)

| | HTTP | ת.ז. | withheld line | `מספר הדירה` | `evidence.read_identifier` |
|---|---|---|---|---|---|
| ADMIN | 200 | shown | — | shown | **0 → 1** |
| OPERATOR | 200 | absent | *נקרא שדה מזהה אחד ואינו מוצג בהרשאה זו.* | shown | **1 → 1** |

Withholding is not a read and writes no line. One row is withheld; the page is not. **The local
extractor is unconfigured**, so the row was inserted against a lease filed through the running
server — capture itself is proved in the suite with a fake extractor. Residue removed: back to
**8 documents / 12 links**, 0 extracted rows, 0 click accounts.

## Found while doing it

**The version window works**, and it cost one confused run: the new suite clocked at 7 Sep read a
catalogue that declares these fields from the 13th, and extraction correctly returned nothing.

**`main` was red for weather.** `routes.test.ts` asserted `doesNotMatch(body, /503/)` against a page
printing a fresh document id — week 5's defect again. The status code already proves it; the body
assertion is now `/unavailable/`, a word no identifier can be.

## Carried → 6.5 · 6.6 · 6.7

- **6.5:** the identifier is not in `proposeLeaseTenancy`'s shape, so the confirm screen cannot leak
  one. 6.5 opens it server-side for identifier-overlap ranking, with its own audit line.
- **6.6:** `extracted_field` is a second home for an identifier — the policy case names the **table**,
  not only `party.national_id`. The registry has a second deliberate exception now,
  `documents · read overlay, may read identifiers`, which 6.6's assertion must name beside the lease
  confirm screen.
- **6.7:** `seed:doctypes` against **staging** before the demo, or the lease type there declares no
  identifier and the walk shows nothing.
- **CSS carry rides on:** `.check` plus the four form classes, two files each — a **third** occurrence
  moves them to `tokens.css`.
