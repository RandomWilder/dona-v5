# Evidence — Slice 6.1 · `estate.write`, and an admin creates a building

**Closed:** 2026-09-13 · **Planned window:** week 6 (Sun 11 Oct) · **Plan mode:** yes. No migration,
no new runtime dependency. Until today every building arrived through an importer or a fixture.

## What was proved

**524** code + **41** hooks + **54** policy · **0 failed** (518/41/54 at 5.8). 4 grep guards, 0
violations. `typecheck` · `lint` · `test` · `test:policy` · `guards` green. Restarted `npm run dev`
twice — pid **12661** for the mockup gate (director approved the paint 13 Sep), pid **14000** for the
wired screen, `/health` 200 both; click-through row deleted afterwards, local DB back at 38.

**ADMIN creates a building.** GET `/estate/buildings/new` 200, 64-char token in the form, project
select from `listProjects`. POST → **303** `/estate` → **1** row, card on the list.

**OPERATOR refused.** POST → **403** `{ code: not_allowed, message: not_allowed }`, 0 rows. **The GET
is 403 too** — a form an operator may fill in and may not post teaches them nothing, because the
refusal says nothing more. צור-בניין link: **0** for that role, **1** for ADMIN.

**Same address twice, one row.** Second post, different name, `"  הדס 3 "`: **1** row, same
`building_id`, name updated (`DO UPDATE`). The guarantee is `building.address_key`, so it holds for
the next writer and the form checks nothing. On `:3000`: **38 → 39** across two posts.

**Derived תקופת הבדק.** Blank field, handover `2025-06-01` → `2027-06-01` (`addCalendarYears`,
`WARRANTY_YEARS = 2`). `DEMOLISHED`, an unknown tender code, `01/03/2025`, a blank name → **400
`invalid`**, 0 rows.

## Red first, and red for the right reason

`estate.write` granted to `OPERATOR` in `MATRIX`, refusal case run → **`actual: 303, expected: 403`**.
Grant removed → 403. A case against an unregistered route would only have proved a 404; this proves
the **matrix line** refuses.

## Three structural calls

- **`csrf: 'in-body'` struck before the code.** `todo.md` specified it; here it is an *exemption*
  from the CSRF `preHandler`, held by `POST /documents` alone (multipart). `src/guard.test.ts`'s
  exempt-list assertion is unchanged and still names one route.
- **The write is `importEstate`** — one plan, zero spaces, zero units; `validateBuildingSpaces`
  iterates both. **No new estate command.**
- **A project is chosen, never typed.** `project_code` is a natural key under `DO UPDATE`: free text
  renames an existing project on a typo. The plan is rebuilt from the chosen row.

## The guard that had never fired

`/dev/mockups/:flow` named two flows in a condition and rendered two TypeScript paints whose slices
(5.3, 5.8) had closed — guard four sat idle over an empty `mockups/` while the paint lived in `src/`.
The route reads the file now (`src/dev-mockups.ts`, live shell; `../package`, `/etc/passwd`, `''` →
404), both paints are deleted, and the live guard riding on the a9 paint (no `asset_type`, no role
matrix on settings) moved onto the **wired** screens in the registry. Guard four ran **live, 1 file**
during this slice; idle again now the paint is gone.

## Carried → 6.2

`config_settings` editor and `DocumentTypeField` stay parked (6.4 seeds fields). Staging `staff:add`
+ the 5.6 clock-end click → **6.7**. `work.ts` → after the console walk. `.form-grid` / `.form-row` /
`.hint` / `.form-actions` now sit in two modules' `styles` blocks; **a third moves them to
`tokens.css`** — watch 6.3.
