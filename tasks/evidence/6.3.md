# Evidence — Slice 6.3 · A12, a document finds its own place

**Closed:** 2026-09-13 · **Planned window:** week 6 (Sun 11 Oct) · **Plan mode:** yes — two modules
and the kernel. No migration, no new dependency, no model call on the intake path.

## What was proved

**547** code + **41** hooks + **54** policy · **0 failed** (527/41/54 at 6.2). 4 grep guards, 0
violations. `typecheck` · `lint` · `test` · `test:policy` · `guards` green. `npm run dev` restarted
twice — pid **22120**, then **22523** after the cap below; `/health` 200 both.

**A lease files itself.** A synthetic lease carrying `כתובת המושכר: רקפת 12, שוהם, דירה 12A`, posted
with **no unit in the form**: **302** → `/documents/…/tenancy` (200), row on `unit 12A`,
`gs://…/unit/01a0774c-188a…/lease/<64 hex>.pdf`, `verified`. Documents **9 → 10**, links **13 → 14**.
Filename `שכירות דוגמה.pdf` — in no path, row or screen.

**An address we do not hold writes nothing.** `אלמוג 5, עיר שאיננה, דירה 3`: **422**, documents and
links **10 / 14 unchanged**, screen naming what was read and offering the search. The bucket half is
proved in the suite, not by a listing — the dev store is memory — with a **put-counting spy** around
the real store: `puts` unchanged across the refusal, the several-candidates case, the VIEWER refusal
and the missing-token refusal. A refusal that wrote the object and skipped the row passes a body
assertion and fails that count.

**The audit line.** One `evidence.intake_unresolved` per refusal — type key, extension, bytes, file
hash, candidate count; no filename, no street, no text. It counts against `UPLOADS_PER_DAY` beside
`evidence.file_document`: 5.2's bound is on what reached intake.

**Red first.** Registering the route turned `src/guard.test.ts`'s exempt list red by itself —
`actual: ['POST /documents', 'POST /documents/intake'], expected: ['POST /documents']`. An allow-list
widens only by somebody editing that line. Edited, with the reason, to two.

## The director's question, and the answer the model already had

*Uploaded to support a tenancy, or attached to an apartment?* Different columns: `storage_uri` names
a **place** (`PlaceKind`, four values), `document_link` carries what it is **about** — `TENANCY`
among its eight. A tenancy has no address, only a unit and two dates, so nothing on a page resolves
to one before extraction, and a lease **creates** the tenancy it supports at A2's confirm screen.
The flat is the anchor: `tenancyId` is null here, and the screen says what comes next.

## Found by clicking, and fixed here

**72 radio buttons** — `דירה 999` at a 72-flat address matches the building and no flat in it, so
every flat is a correct candidate. `CANDIDATE_LIMIT = 12`, real count printed, search box as the way
past. Capped on what is *offered*, never on what is looked up, so it cannot make several into one.

**Test residue**: a case reading its hash back with `rows[0]` and no `ORDER BY` turned one leaked
document into one per run. `src/evidence/routes.test.ts` deletes its own bucket on the way out now.
Local back at **8 documents / 12 links** — one below the 9 it started at, which was a stray.

## The 6.2 carry — it had already tripped

`inTransaction` was written **three** times: `promote.ts`, `lease.ts` byte-identical, inline in
estate's routes. So: `src/kernel/db.ts`, three call sites rewired, estate's twelve lines to one, four
kernel cases (commit order · rollback rethrows · a throwing rollback keeps the original error · a
`PoolClient` passes through). **The CSS half has not tripped** — two files each, carried on.

## Carried → 6.4

A scan pays for OCR **twice**: once in intake for the reader, once inside `fileDocument` on an
`unverified` verdict, because this slice promised to leave that function alone. Fix is to pass the
pre-read pages into the intake request. `mockups/document-intake.html` deleted with this file.
