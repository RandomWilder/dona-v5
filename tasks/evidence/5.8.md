# Evidence — Slice 5.8 · The settings screen — A9, delivered

**Closed:** 2026-09-12 · **Planned window:** week 5 · **Plan mode:** yes (two modules + composition
root). No migration.

> The catalogues were rows. This slice is the hand on them.

## What was proved

**518** code + **41** hooks + **54** policy · **0 failed**. 4 grep guards, 0 violations (mockup
deleted before this file). No new runtime dependency, no migration. `typecheck` · `lint` · `test` ·
`test:policy` · `guards` green.

## The acceptance bar

**Add one of each.** ADMIN POST `/settings/obligation-types` and `/settings/document-types` → 303
to `/settings?saved=…`. Relist shows the codes. Same path on restarted `npm run dev` (pid **21332**,
`:3000` `/health` up; leftover 36285 killed): `CLICK_OB` and `click_dt` both present. No
`asset_type`.

**OPERATOR refused.** POST as OPERATOR → **403** `{ code: not_allowed, message: not_allowed }`.
GET still 200 (`estate.read`); forms absent. No cookie → 303 `/staff/login`. `/dev/mockups/a9`
painted, then the html file deleted.

**Never a third card.** Tokens registry: no `ADMIN` / `VIEWER` / `staff.invite` on the settings
bytes. Role matrix stays code.

## Route map (5.2 carry) — confirmed, not corrected

Public list still **7**. Promote / lease confirm / completeness exception stay `tenancy.write`.
Catalogue POSTs are the first `settings.write` readers. GET `/settings` stays `estate.read`.

## One defect, found at merge rather than at close

`src/settings.test.ts` held a second copy of the never-a-phone guard `/05\d/`, asserted against a
**live** response: **4 failures in 20 runs**, the matching bytes the CSRF token every time (64 hex
characters, so `05`+digit lands about one session in five). Removed rather than the guard widened —
`tests/ui/tokens.test.ts` already renders this screen twice against a fixed `CSRF`. **40/40 after.**

## Carried → 6.1

`config_settings` / secret-name editor. Field schema still seed. Staging add + 5.6 clock-end click. `work.ts` post-7.2.
