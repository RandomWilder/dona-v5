# Evidence — Slice 6.5 · Which tenancy is this? Propose, confirm, write.

**Closed:** 2026-09-14 · **Planned window:** week 6 (Sun 11 Oct) · **Plan mode:** yes — three
modules, one migration, no new dependency. No mockup: the director ruled wire-directly.

## What was proved

**566** code + **41** hooks + **54** policy · **0 failed** (554/41/54 at 6.4), stable over two runs.
4 guards, 0 violations. `typecheck` · `lint` (181 files) · `test` · `test:policy` · `guards` green.
The spec merged first, [#80](https://github.com/RandomWilder/dona-v5/pull/80).

**Red first, four times.** The tenancy read written before its migration: **42883 function
party_national_id_key does not exist** — the failure that says the fold is the database's. Attach
branch disabled → *terms_profile must be 1 to 200 characters*, the create path demanding an annex
attach never uses. Pairing without equal counts → `hasIdentifier` 1, expected 0. Identifier withheld
from `upsertParty` → parties 2873, expected 2872.

**On `:3000`, restarted (pid 34593, `/health` 200), all four cases by clicking.**

| lease | flat | offered | result | parties | tenancies |
|---|---|---|---|---|---|
| A · ת.ז. printed | 1 | new draft | נרשם כטיוטה | 2871 → **2872** | 1674 → 1675 |
| B · same ת.ז., hyphens | 2 | new draft | נרשם כטיוטה | **2872 → 2872** | 1675 → **1676** |
| C · same start date | 1 | letting **pre-selected**, *התאמה לפי ת.ז.: 1* | צורף להשכרה קיימת | 2872 → 2873 | **1676 → 1676** |
| D · no ת.ז. | 3 | new draft, *לא שויכה ת.ז.* | נרשם כטיוטה | 2873 → **2874** | 1676 → 1677 |

**One party, two tenancies** — B added a letting and no person, keyed through a value typed
`312-345-678`. **Attach wrote no dates**: flat 1 still ends **2027-02-28** where lease C said
2027-06-30, still `DRAFT`, `tenancy_party` 1 → 2. **No screen printed an identifier** — 0 hits on all
five pages. `evidence.match_identifier` **13**, `evidence.attach_lease` **1**, **0** carrying a value.
*A first pass asked `inputs ~ '[0-9]{6}'` and found 4: it was reading UUIDs. Week 5's `/05\d/` again.*

## Found by clicking, and fixed here

**The confirm button answered 403 in a browser, and had since 5.2.** Two text-only forms posted
`multipart/form-data` — lease confirm (4.6), promote (4.3) — and 5.2's CSRF `preHandler` reads
`request.body`, which multipart leaves undefined. Never caught because the suite calls those handlers
rather than posting to them. Not a third `csrf: 'in-body'` exemption: those bodies were never
streams. Both drop the `enctype`. **The guard is the class, on the registry** — a form declares that
enctype only with a file input in it — proved red by putting it back.

**121 orphan `terms_profile` rows**, one per run of one `routes.test.ts` case since 4.7, found because
A2's annex list was 125 deep. 6.3's leak in a second table. Cleaned up by the case now; local at **4**.

**A guard on a guard that assumed the data.** The fold-agreement case asserted `party` is non-empty
before comparing every row — true on this machine's 2,871 generated parties, false on CI's empty
database, and red there. It seeds its own six awkward shapes now and asserts the scan covered at
least those, which is the same guard the other way round. Two of the four CI failures this slice had
were mine reading the wrong `tail`; both are in the log.

**A UUIDv7's first eight characters are a timestamp.** `id.slice(0, 8)` as a uniqueness token
collided on `building_address_unique` (23505). Twelve test files use the random tail now. Likewise a
hard-coded ת.ז. in a fixture fails on somebody else's row — identifiers are derived per run.

## Carried → 6.6, and the first is the important one

- **An OPERATOR can read a ת.ז. off the read overlay's word boxes.** The captured-row gate works as
  6.4 claims — admin **1** hit, operator **0** — but the page overlay puts the document's own line in
  a `title` attribute and there both stances score **1**. 6.6 rules: withhold it, or lift the rule
  deliberately.
- **6.6's exception list is one entry, not two.** `LeaseProposal` has no field for an identifier, so
  the read overlay stays the only deliberate exception.
- **Local residue not removed.** `extracted_field`'s promotion guard refuses the delete *and* the
  unstamp — 4.3 working against my own cleanup — and disabling a trigger was blocked and is the
  director's call. Local: **2874** parties, **1677** tenancies, **12** documents, **25** links.
- **CSS carry discharged.** `.check` reached a third file → `tokens.css`. The four form classes are
  still two files each and ride on.
