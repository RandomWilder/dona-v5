# Evidence — Slice 1.2 · Guardrails with teeth

**Date:** 2026-09-04 · **Week 1** (built ahead of Sunday 6 Sep) · **Size:** S

> **Done when:** `rm -rf /`, force push, raw `psql` against prod and `DROP DATABASE` are each blocked
> with exit 2; a write under `src/<module>/` runs that module's tests.
> **Verify:** attempt all four; paste the blocks into the evidence file.

## What was proved

### Red first, then green

`.claude/hooks/hooks.test.mjs` was written **before any hook existed** and run against the empty
directory. The hooks are tested the way Claude Code actually calls them — spawned as a process, fed
the tool payload as JSON on stdin, judged on exit code and stderr — because asserting the regexes
directly would prove the regexes match, not that the guard blocks.

```
$ node --test --test-reporter=tap .claude/hooks/hooks.test.mjs     # before the hooks existed
# tests 29
# pass 0
# fail 29

$ node --test --test-reporter=tap .claude/hooks/hooks.test.mjs     # after
# tests 32
# pass 32
# fail 0
```

The three extra cases are the ones the implementation itself produced: two defects in the lifted v3
code and one whole class of behaviour v3 never had (below).

Two more cases were added in the alignment pass at the end of the slice, and were proved red the
same way — against the already-committed hook, which is the only honest way to show red once the
file exists:

```
$ git show HEAD:.claude/hooks/session-start.mjs > .claude/hooks/session-start.mjs   # old hook back
$ node --test --test-reporter=tap .claude/hooks/hooks.test.mjs
# tests 34
# pass 32
# fail 2

$ node --test --test-reporter=tap .claude/hooks/hooks.test.mjs     # new hook restored
# tests 34
# pass 34
# fail 0
```

### The four blocks, live through the Bash tool

Not simulated. The hook loaded in this session, and these are its verbatim refusals. Each payload was
prefixed with `echo` **on purpose**: the guard matches the command string, so an `echo`-neutered
payload exercises the identical code path while being inert if the hook had *not* been loaded. A
verification that destroys the machine when it fails is not a verification.

```
$ echo rm -rf /
PreToolUse:Bash hook error: Blocked by guard-bash hook: rm -rf on filesystem root.
  Run it manually if truly intended.

$ echo git push --force origin main
PreToolUse:Bash hook error: Blocked by guard-bash hook: force push (use --force-with-lease
  deliberately, outside hooks). Run it manually if truly intended.

$ echo psql "$DATABASE_URL_PROD" -c 'select 1'
PreToolUse:Bash hook error: Blocked by guard-bash hook: raw psql against a prod database.
  Run it manually if truly intended.

$ echo 'DROP DATABASE dona'
PreToolUse:Bash hook error: Blocked by guard-bash hook: DROP DATABASE/SCHEMA.
  Run it manually if truly intended.

$ echo gcloud sql instances delete dona-v5-prod            # the fifth, not in the slice's bar
PreToolUse:Bash hook error: Blocked by guard-bash hook: destructive gcloud command.
  Run it manually if truly intended.
```

Exit 2 is the only code Claude Code reads as *blocked*; 1 is an error the model is shown and may
route around. The suite asserts the code, not just the message.

### A write under `src/<module>/` runs that module's tests

A throwaway `src/estate/` was created with a deliberately failing test, edited through the Edit tool,
and then removed. The module's failure came back **into the transcript, unprompted**:

```
$ (Edit src/estate/probe.ts)
PostToolUse:Edit hook: ✖ space kinds are the six from SPEC.md rule 6 (0.469291ms)
  ℹ tests 1 · pass 0 · fail 1
  AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:
      5 !== 6
      at src/estate/probe.test.ts:7:3
  Focused tests failing in src/estate after this edit.
```

The green path was proved in the same way and in the same session: with the assertion corrected, the
next write produced **no output at all**. Both directions, not just the loud one. `src/` was then
deleted — the kernel does not land until 1.4.

### SessionStart

```
$ node .claude/hooks/session-start.mjs
## slice/1.2-guardrails...origin/slice/1.2-guardrails

tests: no toolchain yet — `npm test` lands in slice 1.3 (AGENTS.md status line).
guardrails: armed — guard-bash · after-write
```

The last line is the alignment pass's answer to raised item 3. It names the hooks that are **both**
registered in `settings.json` and present on disk, so registered-but-deleted — what a rebase
produces — reads `guardrails: INCOMPLETE — guard-bash loaded`, which has its own case. A session that
declines `.claude/settings.json` prints nothing at all, and that absence is the tell. It is a tell
and not an enforcement: nothing inside the repo can detect a hook that never ran, because the
detector would itself be a hook. Stated here rather than left to look like a control.

## Three defects in the lift, found by verifying rather than by reading

`docs/from-v3.md` files these hooks under Tier 1, "take verbatim". **Two of the three would have been
silently broken in v5, and one of them was already silently broken in v3.** This is the slice's most
useful output.

**1. `node --test src/<module>` does not discover tests under Node 24.** A positional argument is a
file or a glob, not a directory to walk:

```
$ node --test src/estate
Error: Cannot find module '.../src/estate'   [MODULE_NOT_FOUND]     exit 1
```

v3's hook read that non-zero exit as "the module's tests failed" and would have printed
`Focused tests failing in src/estate` after **every single edit**, whatever the tests actually did. A
guardrail that fires constantly is a guardrail you learn to scroll past. v5 collects the test files
with `fs.globSync` and runs them explicitly, which also makes *"this module has no tests yet"* — the
ordinary state of a module in week 1 — a silent no-op instead of an error.

**2. A `PostToolUse` hook that exits 0 has its stderr hidden from the agent.** v3's `after-write.mjs`
wrote its failing-test report to `console.error` and then `process.exit(0)`. **Nothing ever read it.**
The feedback loop the file's own header describes — "feedback in seconds, not at push time" — did not
exist. Exit 2 is the code that puts stderr in front of the model; it does not undo the write, because
`PostToolUse` fires after the tool has already run. This is the change that gives the slice its title,
and it is why the second half of the bar is demonstrated with a transcript paste rather than a claim.

**3. `rm -rf /*` was not matched.** v3's `\/(\s|$)` requires the slash to end the argument, so the
canonical `rm -rf /` was blocked and the equally terminal `rm -rf /*` was not. The pattern now also
accepts `*` and `--no-preserve-root`.

## What the guard deliberately does not do

Pinned as passing tests under *accepted false positives*, so the next person meets them as a recorded
decision rather than as a surprise at 2am:

| Command | Why it is blocked anyway |
|---|---|
| `grep -rn "DROP DATABASE" migrations/` | The guard matches the string, not the intent |
| `git push --force-with-lease origin <branch>` | `--force` is a prefix of it; the safe form goes with the unsafe one |
| `psql -h 127.0.0.1 dona_dev -f seed-production-catalogue.sql` | "prod" anywhere on the line |

A fourth one arrived unbidden and is worth more than the three predicted ones: **this slice's own
commit was blocked.** The message quoted the blocked commands literally, `git commit -m` puts the
message on the command line, and the guard refused it. Fixed by passing the message as a file, which
is the better habit anyway. The guard cannot tell a command from prose about a command, and now there
is a dated instance of that rather than a hypothesis.

The trade is deliberate and asymmetric: a false positive costs one rephrase, a false negative costs a
database.

**Known gaps, stated rather than discovered.** `git push origin +main` is a force push by refspec and
is not caught. `rm --force -r /` spells the flags in a way the pattern misses. And the whole class is
bypassable by construction — the guard sees one command string, so `bash -c`, a shell script, a
base64 payload or a here-doc all walk past it. **This is a net against accidents and momentum, not
against an adversary**, and no line in `SPEC.md` should ever be defended by it alone. The constraints
that must hold against intent live in `tests/policy/` (1.7) and in database constraints.

## The allowlist

`.claude/settings.json` is now tracked (`.gitignore` excludes only `settings.local.json`). Seventeen
routine commands are allowlisted so flow is uninterrupted — tests, both gates, `measure`, typecheck,
lint, format, `node --test`, `npx biome`, `git status`/`diff`/`log`/`add`, `db:up`, docker compose
up/ps/logs. **Eight of them are `npm run` scripts and there is no `package.json` yet** — none of them
exist until 1.3, 1.7 and 1.8. An allowlist entry for a missing script is inert, and writing them now
means the toolchain slice does not have to remember to.

Deliberately **not** allowlisted, and each stays behind a prompt: `git commit`, `git push`, `gh api`
and `gh pr` (they write), anything `gcloud`, and the deploy and rollback scripts. Pipeline §4: the
routine is allowlisted, deploys and destructive commands are not.

## Raised, and each one given an owner before the slice closed

The first draft of this file listed four open items here and stopped. That is the failure mode this
slice ended up fixing, so the list now carries dispositions instead of observations. Two were closed
inside 1.2; two were written into the entries of the slices that can close them.

| # | Item | Disposition |
|---|---|---|
| 1 | The 34 hook cases are in no suite — `node --test .claude/hooks/hooks.test.mjs` is the only thing that runs them | **Written into 1.3**, `todo.md` and `roadmap.md`. It cannot close earlier: there is no `package.json` to add `npm test` to. Slice 1.3 also owes `after-write.mjs` the Biome step pipeline §4 specifies, which was unbuildable before Biome existed |
| 2 | `AGENTS.md` at 40 lines against pipeline §3's "20–30 lines MAX" | **Closed here.** Now 30. The overflow was duplication of `SPEC.md`, so the content moved rather than went: migration conventions, the injected clock and the UI/RTL token rules are now a *Code conventions* section in `SPEC.md`, and the plan-mode trigger was already stated verbatim in `CLAUDE.md`. The cap's reason is that duplicated context measurably degrades agent performance — deleting a duplicate serves it, deleting a rule would not |
| 3 | A session that declines `.claude/settings.json` has no guardrails and looks identical to one that has them | **Mitigated here**, honestly labelled. `SessionStart` now prints `guardrails: armed — guard-bash · after-write`, with a case for the registered-but-missing state. Undetectable from inside the repo by construction; this makes it visible, not impossible |
| 4 | The guard covers the **Bash tool only** — Write, Edit and every MCP tool reach the filesystem without it | **Written into 1.12**, both files. The corpus controls must rest on `.gitignore`, bucket IAM and the policy suite, never on this hook |

### Two items 1.1 raised that had already been lost

Found while auditing the above, and the reason the carry rule now exists. Slice 1.1's evidence raised
three items. One reached the plan; **two existed nowhere but in a file nobody reopens.**

- **The required check names `gate` and `evals` are load-bearing.** Branch protection requires those
  exact contexts, so a CI job named anything else leaves every PR blocked forever with no check ever
  reporting. 1.1 called it the sharpest edge in the repo and it was invisible to 1.6, the slice that
  will trip it. **Now written into 1.6** in `todo.md` and `roadmap.md`.
- **ADR-0004's disclosure obligation belonged on `fuses.md`** and had no row. It is a fuse in the
  strict sense — someone else's decision, with a burn time we do not control, and a third party
  discovered after the fact is a data-custody incident rather than a config edit. **Now `F6`**, owed
  before the tier-2 corpus lands in 1.12.

### The rule that came out of it

Written into [pipeline.md](../../docs/pipeline.md) §8 step 7, the Definition of Done in
[plan.md](../plan.md), and [CLAUDE.md](../../CLAUDE.md): **a slice is closed when nothing it raised
is unowned** — carried into the entry of the slice that closes it, in `todo.md` and `roadmap.md`, not
only into evidence. The bar is deliberately not *nothing open*: week 1 is a dependency chain and 1.2
cannot join a test script that 1.3 creates. Chasing zero-open would mean either lying or collapsing
the week into one unverifiable slice. Carrying an item costs one sentence read at the moment it
matters; losing one costs whatever it breaks. §10 gains the matching anti-pattern.

## Raised and settled

- **A harness artefact, not a hook defect.** The first green run was a lie: `NODE_TEST_CONTEXT=child-v8`
  is inherited from the test runner into the spawned hook and on into the hook's own `node --test`
  grandchild, which then reported over *this* runner's IPC channel and exited 0 — so a failing module
  test looked green to the hook. The child environment is scrubbed in the test helper, with the reason
  written beside it. Claude Code never spawns a hook from inside a test runner, so the hook itself
  needs no defence; the suite that tests it does.
- **No `SPEC-<module>.md` was touched.** This slice changes no module behaviour. The Definition of
  Done's spec-before-code bullet is not applicable rather than skipped.
- **Most of the standing Definition of Done cannot be run yet, by sequence and not by omission:**
  `typecheck` / `lint` / `npm test` land in 1.3, `tests/policy/` in 1.7, `npm run evals` in 1.8, and
  staging plus `infra/smoke.sh` in 1.6. What was available — red-first proof, the diff read, an
  evidence file, no new runtime dependency — was done.

## Where it stands

PR **#2** (`slice/1.2-guardrails` → `main`), reporting exactly what 1.1 predicted it would:

```
$ gh pr view 2 --json mergeStateStatus,mergeable,statusCheckRollup
{"mergeStateStatus":"BLOCKED","mergeable":"MERGEABLE","statusCheckRollup":[]}
```

`gate` and `evals` are required and no workflow exists to report them until 1.6, so this slice
**cannot merge through the front door.**

**Decided: merged on the admin bypass, 4 Sep 2026.** Today the bypass skips nothing, because there is
nothing to skip — the two required contexts have no workflow behind them, so the merge walks past a
check that does not exist rather than one that would have caught something. Waiting is the worse
option and does not avoid the exception: 1.3 depends on 1.2, 1.4 on 1.3, 1.6 on 1.3 and 1.5, so
holding this branch stacks four slices into a single big-bang integration at the exact commit that
introduces CI — and **1.6's own Verify requires pushing a red commit directly to `main` anyway.**
Waiting would not remove the bypass, only concentrate it.

**And the window it lives in was shortened.** 1.1 owed the `enforce_admins: true` flip to **1.10**,
for the sound reason that 1.6 must push red to `main`. But that reason expires the moment 1.6's
Verify has run, and as written it left **1.7, 1.8 and 1.9 merging in a world where the checks are
real and an admin can still walk past them** — four slices of genuine exposure against zero today.
The flip is now the last act of **1.6**; 1.10 confirms it rather than owning it, since 1.10 is the
first slice that runs entirely inside the enforced gate and its break→blocked leg proves the flip
took. `todo.md` and `roadmap.md` both carry the move.

This is the second recorded use of the bypass, and the last one that should ever be needed.

## What this cost

One session. **No runtime dependencies and no `package.json`** — the hooks are three `.mjs` files on
Node 24's standard library (`node:child_process`, `node:fs`, `node:path`, `node:test`). Four files
added under `.claude/`, one checkbox in `todo.md`.

The alignment pass at the end of the slice cost the same session again and touched seven more files,
none of them code: `AGENTS.md` cut from 40 lines to 30 with the overflow moved into `SPEC.md`;
`todo.md` and `roadmap.md` given the owed items for 1.3, 1.6, 1.10 and 1.12; `fuses.md` given F6;
`pipeline.md` given the carry rule (§8), the exit-code fact (§4) and two anti-patterns (§10);
`plan.md` given the matching Definition of Done bullet; `CLAUDE.md` given both standing rules. That
is the slice's real output alongside the hooks: **1.1's evidence was written well and read once, and
two of its three findings were gone a slice later.** The hooks stop a bad command. The carry rule
stops a good finding from evaporating, which on the evidence so far is the likelier failure.
