# Slice 6.9 — The document tab, and a refusal that offers to create

**Closed 14 Sep 2026.** Flow A12 gains its entrance and its missing screens. Branch `slice/6.9-document-tab`.

**The tab.** `ChromeDest` was seven destinations and none was documents, so A12 — built at 6.3 and
working — had one door, a card on the index, and the week-6 demo never passed the index again: six
posts through A1's unit-first screen and none through A12. `signedInChrome` takes `mayFile` now and
the item renders only for `documents.write`. On `:3000`: present on `/estate` and `/settings` for an
ADMIN, **0** for an OPERATOR, `/documents/new` one click away with no building in the path, and
`aria-current` on documents screens, which said `estate` until here. Required and not defaulted, for
5.8's reason — four test files and three dep types had to be told, which is the parameter working.

**Both stances, one request.** Written **red first**: 3 of 4 cases failed against `main`; the OPERATOR
case passed vacuously. Same lease, `דקל 9, כפר סבא, דירה 14`, in nobody's portfolio:

| | ADMIN | OPERATOR |
|---|---|---|
| status · create links · search box · file re-armed | 422 · **1** · 1 · 1 | 422 · **0** · 1 · 1 |
| `document` rows · objects put | 0 · 0 | 0 · 0 |

The walk, clicked after a `npm run dev` restart: refusal → building form carrying `value="דקל 9"`,
`value="כפר סבא"` and three hidden inputs → 303 → flat form carrying `value="14"` → 303 →
`/documents/new?anchor=…&type=lease`, radio **checked**, type selected → same lease filed, **302 into
`/documents/:id/tenancy`**, `document` 12 → 13, no address retyped anywhere.

**Four causes, four sentences.** Each on its own paper, live: nothing read → `לא נקראה כתובת מן הדף`;
**annex** → `הנכס מתואר בנספח · והנספח אינו נקרא — זו החלטה ולא תקלה`, 6.11's ruling said out loud, and
no create control because nothing was read to prefill; not in the portfolio → `הכתובת הזו אינה בתיק`
plus the building-and-flat offer; building held → `הבניין נמצא בתיק, והדירה לא` plus **only** the flat
offer (0 building links). 6.11's open half is printed now: `נקרא מספר דירה 206-7, אך ללא כתובת — ייתכן
שנקרא משורה של אחד הצדדים`. The confirm screen split the same way — `crossCheck` returns the four facts
and `matchesUnit` stays their conjunction, so **what writes is unchanged**; live it says
`לא נקראה כתובת מן המסמך` + `לא נקרא מספר דירה מן המסמך`, and the old `אינם תואמים` is gone.

**The receipt:** `נקרא מן המסמך: דקל 9, כפר סבא · דירה 14. תויק לדירה 14 …` — on an arnona bill,
because a verified lease redirects past this screen into A2, whose confirm page carries the lease's half.

**Numbers.** `npm test` 611 + 41 pass, 0 fail · `test:policy` 60 · `guards` 4 ok · `lint` 184 files ·
`evals` 1/3, 2 skipped, unchanged — no prompt, model id, retrieval config or tool definition moved.
**The developer database is as this slice found it**, 12 documents before and after: the first week-6
slice to leave no residue, because nothing it wrote was stamped by the promotion guard.

## Raised

- **→ 6.10:** `.check` is in `tokens.css` already; `.form-grid` / `.form-row` / `.hint` /
  `.form-actions` are still **two files each** and a third occurrence moves them down. This slice
  wrote screens in exactly those two files and did not trip it.
- **→ the director:** the rail's other destinations are **not** gated — `settings` is ADMIN-only and a
  VIEWER is shown it; `staff` and `calls` are their own question. Older than this slice, not widened
  by it, and gating them is a ruling.
- **→ whichever slice repaints a flow:** `MOCKUP_OWNERS` is live code and said `document-intake: 6.3`,
  so a repaint fails guard four against a two-week-old evidence file. Moved to `6.9`; the owner is
  always the slice that will wire it next.
- **→ the corpus item (F6):** the apartment number is still *read* from anywhere in the text. 6.9 says
  where it may have come from; a second real form is what would stop it.
