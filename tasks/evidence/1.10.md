# Evidence — Slice 1.10 · Prove the pipeline in both directions, on purpose

**Date:** 2026-09-05 · **Week 1** (built ahead of Sunday 6 Sep) · **Size:** S

> **Done when:** the round trip is complete and the post-rollback deploy serves 100%, not 0%.
> **Verify:** revision list with traffic percentages at each step.

`docs/pipeline.md` §9's last line, carried unchecked since day one, and the only line on it that has
to be *performed* rather than built. The week to do it is the week nothing depends on prod: the cost
of being wrong here is a revision list, and in week 12 it is a pilot building.

## The round trip, in one table

```
leg                          artefact                                  result
1  break                     PR #15, red on purpose                    BLOCKED · admin merge refused
2  fix → merge → staging     7a2569f                                   dona-staging-00010-dqb · 100%
3  tag v0.1.0 → prod         first ever run of release.yml             dona-prod-00001-ldz · 100%
4  tag v0.1.1 → prod         second release                            (below)
5  roll prod back            infra/rollback.sh prod                    (below)
6  tag v0.1.2 → prod         the post-rollback deploy                  (below)
7  park prod                 database stopped, service scaled to zero  (below)
```

## Leg 1 — a red PR is blocked, and an admin cannot push past it

`tests/pipeline-proof.test.ts` asserted `'red' === 'green'` and nothing else. It existed for two
commits and was deleted by the second.

```
gate    33976241267/job  FAILURE   ℹ tests 154 · pass 153 · fail 1
evals   33976241267/job  SUCCESS   ← the other required context stayed green: this is one test, not an outage
mergeStateStatus         BLOCKED
```

The failure itself, from the job log:

```
✖ fails on purpose so the gate can be seen refusing a merge (2.370808ms)
  AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:
    expected: 'green',
```

**And the half that is new.** 1.6 flipped `enforce_admins: true` as its last act and no slice had
tested it from the outside. Both merge paths were attempted, by the account that owns the repository:

```
$ gh pr merge 15 --squash
X Pull request RandomWilder/dona-v5#15 is not mergeable: the base branch policy prohibits the merge.

$ gh pr merge 15 --squash --admin
GraphQL: Required status check "gate" is failing. (mergePullRequest)
```

That second line is the whole of 1.1's last carry, proved. In 1.1 the same command was the *only* way
through, against an empty rollup; here it is refused against a rollup with a real failure in it.

## Leg 2 — the fix merges and staging moves on its own

Deleting the file was the entire fix; the break was never about the code. `package.json` went
`0.1.0-dev` → `0.1.0` in the same commit, because that commit is what `v0.1.0` points at.

```
CI      33976313614  success  d13498e  46 s   ← branch, both contexts green
CI      33976362593  success  7a2569f  42 s   ← main, after the merge
Deploy  33976397567  success  7a2569f  1 m 44 s
```

```
dona-staging-00010-dqb   100%   {"ok":true,"version":"7a2569f","db":"up"}
dona-staging-00009-wpw     0%   c314f03   ← the previous slice's tip
```

## Leg 3 — `v0.1.0`, and the first execution of a workflow written three slices ago

`release.yml` was written at 1.6 and had never run. Nor had the two lines 1.8 added to it. The tag
was pushed at 16:00:17Z and prod was serving at 16:02:37Z — **2 m 20 s**, first run included.

```
gate / gate    success   16:00:30 → 16:01:01   31 s
gate / evals   success   16:00:23 → 16:01:00   37 s   ← needs OPENAI_API_KEY, which a called
prod           success   16:01:03 → 16:02:37   1 m 34 s   workflow inherits only via `secrets: inherit`
```

Both halves of the gate ran against the tagged commit through `workflow_call`, which is what makes a
release something other than a redeploy of whatever CI last happened to bless. A red `evals` would
have stopped the release before prod, and that path had never been executed until this tag.

**The migration ran as `app-prod` against a virgin production database, before the revision served:**

```
2026-09-05T16:02:11Z  migrate: applied 4
                        0001_init.sql
                        0002_kernel_durability.sql
                        0003_kernel_settings.sql
                        0004_estate.sql          ← the estate spine, landed at 1.9
                      Container called exit(0).
```

Four, not 1.6's three: prod's first migration run is the first time `0004_estate.sql` has been
applied anywhere but staging and a laptop. The job `dona-prod-migrate` and the service `dona-prod`
were both **created by this run** — bootstrap deliberately creates neither.

```
https://dona-prod-r44j24yuaa-zf.a.run.app/health → {"ok":true,"version":"v0.1.0","db":"up"}
dona-prod-00001-ldz   100%   VERSION=v0.1.0
```

`VERSION` is the tag on prod and the short commit on staging, which is the distinction the two
workflows draw deliberately: a release is named by what was released, a staging revision by what
happened to merge.
