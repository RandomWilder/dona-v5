# Evidence — Slice 4.7 · A3: an addendum completes a tenancy

**Date:** 2026-09-08 · **Week 4** · **Size:** S

> **Done when:** a guarantor named in an addendum becomes a `tenancy_party` under the existing
> tenancy, and a later date overwrites an earlier one without deleting the earlier provenance.
> **Verify:** addendum after a lease, same path as 4.6; both provenances on screen.

## The numbers

```
                               before (4.6b)           after (4.7)
npm test — code                440                     445          (+5)
npm test — hooks               41                      41
tests/policy/                  44                      44           (no new case)
biome check                    145 files               146 files
guards                         3 · ok                  3 · ok       (19 migrations scanned)
typecheck                      clean                   clean
runtime dependencies           5                       5
migrations                     19                      19           (none)
evals                          unchanged               not re-run   (no prompt/tool change)
```

Five new tests: guarantor on the existing tenancy and a second confirm is a no-op; later
`new_end_date` updates the column, keeps both stamped captures, two `tenancy_event` rows; unbound
addendum is `invalid` and writes no party; HTTP 302 to `/documents/:id/tenancy`; confirm HTML has no
terms-profile select.

## What was proved

- **Same path as A2.** Verified `lease_amendment` filed against a letting redirects to
  `/documents/:id/tenancy`. No second mechanism. Without a letting the file stays on the filed page
  and confirm is `invalid`.
- **The TENANCY link is the target, not the done flag.** Upload already writes `EVIDENCE` when a
  letting is chosen. A2's "already has a TENANCY link" check would have no-op'd the addendum. Done
  is an `evidence.confirm_amendment` audit line on that document.
- **Guarantor.** One captured `guarantor_name` becomes one `GUARANTOR` with `is_service_contact`
  false on the existing tenancy. Zero guarantors plus a date change is success. No terms profile.
- **Later date wins; earlier capture stays.** Promoting `new_end_date` copies onto `tenancy.end_date`
  and appends `tenancy_event`. The lease's stamped `end_date` is not un-stamped. The unit list of
  stamped fields includes both values.
- **A4 is not this slice.** Completeness (at least one guarantor) stays a 4.8 policy case over saved
  rows.

## Verify

```
npm run typecheck                          ok
npm run lint                              146 files
npm run guards                             3 guards, 0 violations
npm test                                   445 code + 41 hooks, 0 fail
npm run test:policy                        44, 0 fail
  guarantor on existing tenancy            1 GUARANTOR, is_service_contact false
  second confirm                           alreadyEstablished, still 1
  new_end_date 2028-02-28                  tenancy.end_date that day; 2 events; 2 stamped values
  unbound addendum                         invalid, 0 GUARANTOR on that unit
  HTTP addendum + letting                  302 /documents/:id/tenancy
```

`npm run dev` restarted on this working copy (`db:up`, extract unconfigured). Browser: דירה 1,
אלון 12 — type list includes נספח לחוזה שכירות; letting `2025-05-03 — 2027-05-03 · פעיל` selectable.
A live Hebrew addendum PDF was not posted: the native reader rejects the test stub PDF, extract is
unconfigured, and the write path is the five tests above.

## Carry

- A4 incomplete-tenancy queue — **4.8**. Zero-guarantor drafts stay legal until then.
- Staging `ocr:sweep` — still director after a serving revision.
- F6's three acts — block **4.5** only.
- Which `terms_profile` a real lease is on — still week 5 / 2.5.
- Placeholder bays vs a real register's bay column — **2.5**.
