# Evidence — Slice 5.4 · Session unlocks evidence

**Closed:** 2026-09-10 · **Planned window:** week 5 · **Plan mode:** yes.

> Three things 3.1/3.2/3.6 held until an authenticated actor existed. This slice spends the session.

## What was proved

| | |
|---|---|
| Tests at close | **500** code + **41** hooks + **54** policy · **0 failed** |
| Grep guards | 4, 0 violations (mockup guard idle) |
| New runtime dependencies | **none** |
| New migration | **`0024_document_uploaded_by.sql`** — nullable `uploaded_by` FK. 26 tables still. |

`npm run typecheck` · `lint` · `test` · `test:policy` · `guards` green. Public routes still **7**.

## The acceptance bar

**Uploader.** HTTP ingest writes `uploaded_by` from the session. Seed/importer stay null. Same-hash
re-ingest keeps the first operator.

**Signed URL.** Panel mints a 15-minute GCS V4 GET. Search does not. `expiresAt <= now` → `invalid`.
URL never in `audit_log`.

**`superseded_by` still omitted.** Verify DB: **6** mapping rows, **2** targets (`start_date` × 2,
`end_date` × 4), **0** live stamps. Invariant 2 and `valid_from`/`valid_to` still cover the cases.

**Never-a-name KEPT.** Same reasons as 5.2. HTTP `promoted_by` / `confirmed_by` / incomplete `actor`
= operator email (snapshot), no typed name field.

**`:3000`.** Unit with no cookie → login. Href proved in token + routes tests. Local store is
**memory** (not live GCS). Opening bytes is staging after `bootstrap.sh staging` grants
`serviceAccountTokenCreator`. Restart `npm run dev` before a local click of הורדה — the listener
on `:3000` in this session predated the slice.

## Carried → 5.5

If portfolio promotion finds a case this ruling does not cover, **5.5 owns `superseded_by`.**
