# Evidence — Slice 4.6a · Lease place fields: building number is not the flat

**Date:** 2026-09-08 · **Week 4** · **Size:** S

> **Done when:** the extractor is instructed not to swap בניין מספר and דירה מספר; DATE capture is
> ISO `YYYY-MM-DD`; lease hints from 2026-09-08 say both; golden set still green.
> **Verify:** extract tests capture the instructions and hints, drop a Hebrew DATE, keep ISO;
> `npm run evals`.

## The numbers

```
                               before (4.6)            after (4.6a)
npm test — code                434                     437
npm test — hooks               41                      41
tests/policy/                  44                      44           (no new case)
biome check                    clean                   clean
guards                         3 · ok                  3 · ok       (19 migrations scanned)
typecheck                      clean                   clean
evals                          1/3 passed, 2 skipped   same         (prompt of the mapping call, not the agent)
migrations                     19                      19           (none — catalogue seed)
```

The code count is the suite after this change (`437` pass).

## What was proved

- **Instructions.** `EXTRACT_INSTRUCTIONS` tells the mapping model that בניין מספר belongs in
  `address` and דירה מספר is `apartment_number` only; חניה is ignored; DATE values are ISO
  `YYYY-MM-DD`. A fake extractor captures the exact string.
- **R18.** The 2026-09-07 hints on place and date fields close on that day. A 2026-09-08
  declaration carries the distinguishing hint. Same `field_key`, new row, not an in-place edit.
- **DATE drop.** Hebrew month names and `dd/mm/yyyy` are not stored on a DATE field. An ISO day is.
  TEXT is untouched. Same rule FieldPromotion already uses.
- **Clock.** Extraction on 2026-09-08 reads the new hints. Earlier clocks still see the 2026-09-07
  wording.
- **Cross-check unchanged.** A remaining swap is still `invalid` at confirm.

## Carry

- **Staging** needs the new revision **and** `npm run seed:doctypes` (catalogue is not on deploy).
  Then unfile the דירה 4 lease and upload again — the same hash will not re-extract.
- A3 addendum — **4.7**.
- Confirm picks terms profile from the list — **4.6b**.
- Staging `ocr:sweep` — still director.
- F6's three acts — block **4.5** only.
