# Evidence — Slice 4.8 · A4: the incomplete-tenancy queue

**Date:** 2026-09-08 · **Week 4** · **Size:** M

> **Done when:** a tenancy that extraction returned with zero guarantors appears in the queue
> showing what is missing; an addendum (4.7) or a recorded exception clears it.
> **Verify:** policy case red first; lease with zero guarantors in the queue; A3 removes it.

## The numbers

```
                               before (4.7)            after (4.8)
npm test — code                445                     450          (+5)
npm test — hooks               41                      41
tests/policy/                  44                      47           (+3)
biome check                    146 files               148 files
guards                         3 · ok                  3 · ok       (20 migrations scanned)
typecheck                      clean                   clean
runtime dependencies           5                       5
migrations                     19                      20           (0020_tenancy_completeness)
evals                          unchanged               not re-run   (no prompt/tool change)
```

Five new tests in `npm test`: three policy cases (also in `test:policy`), one schema case on the
exception table, one HTTP case that lists then clears via POST. A3's existing confirm case now
asserts the queue before and after the guarantor write.

## Red first

Against `src/tenancy/contract.ts` before the exports existed:

```
SyntaxError: The requested module '../../src/tenancy/contract.ts' does not provide an export named 'listIncompleteTenancies'
✖ tests/policy/completeness.test.ts
ℹ fail 1
```

Then the migration and the two commands. The three cases went green. A tenancy with tenants and
zero guarantors still inserts (not `23502`, not `23514`).

## What was proved

- **Never a NOT NULL.** Zero `GUARANTOR` rows remain a legal tenancy. Completeness is
  `listIncompleteTenancies`: `DRAFT` or `ACTIVE`, a `TENANCY` document link, no guarantor, no
  exception for rule `guarantor`. Register lettings with no such link stay off the list.
- **Exception is a row**, `(tenancy_id, rule)`, clock `at`, no `DEFAULT now()`. A second post of
  the same pair is `alreadyRecorded`. Not a status column on `tenancy`.
- **A3 clears it.** Confirm of an addendum that writes a `GUARANTOR` removes that tenancy from the
  query. No extra hook on confirm.
- **Queue.** `GET /estate/incomplete` shows חסר ערב and the TENANCY-linked document (a `lease`
  type wins). Estate does not import tenancy; `app.ts` injects the commands. No party names.

## Verify

```
npm run typecheck                          ok
npm run lint                              148 files
npm run guards                             3 guards, 0 violations (20 migrations)
npm test                                   450 code + 41 hooks, 0 fail
npm run test:policy                        47, 0 fail
  insert with zero ערב                     stored
  document-backed draft listed             missing=guarantor; register-only and with-ערב excluded
  exception                                recorded then alreadyRecorded; gone from the list
  HTTP POST exception                      302 /estate/incomplete; unit href gone
  A3 confirm                               in the list after the lease, out after the ערב
```

`npm run dev` restarted on this working copy (`db:up`, extract unconfigured). Browser:
`/estate/incomplete` — 2 rows, דירה 4 and דירה 5, בניין האלון 12, חסר ערב, document labelled
חוזה שכירות. Unit link opened דירה 4. Did not POST an exception against those live rows; the
write path is the HTTP test. Document `/read` on this process returned `object not found` because
`npm run dev` is on the memory object store — the queue named the document; the bytes are not in
this process.

## Carry

- Staging `ocr:sweep` — still director after a serving revision.
- F6's three acts — block **4.5** only.
- Which `terms_profile` a real lease is on — still week 5 / 2.5.
- Placeholder bays vs a real register's bay column — **2.5**.
- Completeness stays derived until a second rule joins the guarantor rule.
